<%= form_with(model: pet_sitter) do |form| %>
  <% if pet_sitter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet_sitter.errors.count, "error") %> prohibited this pet_sitter from being saved:</h2>

      <ul>
        <% pet_sitter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
